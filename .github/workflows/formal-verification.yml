name: Formal Verification

on:
  push:
    branches: [main]
    paths:
      - 'formal_verification/**'
      - 'crates/shielded/**'
  pull_request:
    branches: [main]
    paths:
      - 'formal_verification/**'
      - 'crates/shielded/**'

jobs:
  lean4:
    name: Lean 4 Proofs
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: formal_verification/lean

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Cache mathlib
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/mathlib
            formal_verification/lean/.lake
          key: ${{ runner.os }}-mathlib-${{ hashFiles('formal_verification/lean/lakefile.toml') }}
          restore-keys: |
            ${{ runner.os }}-mathlib-

      - name: Fetch mathlib cache
        run: |
          lake exe cache get || true

      - name: Build
        run: lake build

      - name: Verify no sorry
        run: |
          if grep -r "sorry" CallchainShielded/; then
            echo "Found sorry in proofs"
            exit 1
          fi

      - name: Verify axiom count
        run: |
          AXIOM_COUNT=$(grep -c "axiom" CallchainShielded/Fr.lean || true)
          if [ "$AXIOM_COUNT" -gt 2 ]; then
            echo "Too many axioms: $AXIOM_COUNT (expected <= 2)"
            exit 1
          fi
          echo "Axiom count: $AXIOM_COUNT (OK)"

      - name: Test
        run: lake test || true
