name: Formal Verification

on:
  push:
    branches: [main]
    paths:
      - 'formal_verification/**'
      - 'crates/shielded/**'
  pull_request:
    branches: [main]
    paths:
      - 'formal_verification/**'
      - 'crates/shielded/**'

jobs:
  r1cs-export:
    name: R1CS Export & Structural Verification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-action@stable

      - name: Export R1CS circuits
        run: cargo run --release --features real-prover --bin export_r1cs
        working-directory: crates/shielded

      - name: Verify R1CS structural correspondence
        run: cargo run --release --features real-prover --bin verify_r1cs
        working-directory: crates/shielded

      - name: Upload R1CS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: r1cs-export
          path: r1cs/*.json

  lean4:
    name: Lean 4 Proofs
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: formal_verification/lean

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Cache mathlib
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/mathlib
            formal_verification/lean/.lake
          key: ${{ runner.os }}-mathlib-${{ hashFiles('formal_verification/lean/lakefile.toml') }}
          restore-keys: |
            ${{ runner.os }}-mathlib-

      - name: Fetch mathlib cache
        run: |
          lake exe cache get || true

      - name: Build
        run: lake build

      - name: Verify no sorry
        run: |
          if grep -r "sorry" CallchainShielded/; then
            echo "Found sorry in proofs"
            exit 1
          fi

      - name: Verify axiom count
        run: |
          # Fr.lean should have exactly 1 mathematical axiom (BN254_P_prime)
          FR_AXIOMS=$(grep -c "axiom" CallchainShielded/Fr.lean || true)
          if [ "$FR_AXIOMS" -gt 1 ]; then
            echo "Too many axioms in Fr.lean: $FR_AXIOMS (expected <= 1)"
            exit 1
          fi

          # R1CSCorrespondence.lean has 3 externally-verified axioms (structural correspondence)
          CORR_AXIOMS=$(grep -c "axiom" CallchainShielded/R1CSCorrespondence.lean || true)
          if [ "$CORR_AXIOMS" -gt 3 ]; then
            echo "Too many axioms in R1CSCorrespondence.lean: $CORR_AXIOMS (expected <= 3)"
            exit 1
          fi

          TOTAL=$((FR_AXIOMS + CORR_AXIOMS))
          echo "Axiom count: Fr.lean=$FR_AXIOMS, R1CSCorrespondence.lean=$CORR_AXIOMS, total=$TOTAL (OK)"

      - name: Test
        run: lake test || true
